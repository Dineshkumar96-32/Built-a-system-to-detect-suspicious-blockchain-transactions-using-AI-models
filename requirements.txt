# ── Web Framework ──────────────────────────────────────────────────────────────
fastapi>=0.115.0
python-multipart>=0.0.12
uvicorn[standard]>=0.32.0
websockets>=13.0

# ── Database & Caching ─────────────────────────────────────────────────────────
aioredis>=2.0.1
aiosqlite>=0.20.0
alembic>=1.13.1
asyncpg>=0.30.0          # async PostgreSQL driver
redis>=5.0.4
sqlalchemy>=2.0.35

# ── Blockchain ─────────────────────────────────────────────────────────────────
eth-abi
eth-account
web3

# ── Machine Learning ───────────────────────────────────────────────────────────
joblib           # model serialization
networkx           # graph analysis for wallet clustering
numpy>=2.1.0
pandas>=2.2.0
scikit-learn>=1.5.0
scipy>=1.14.0

# ── Async & Streaming ──────────────────────────────────────────────────────────
aiohttp>=3.10.5
anyio>=4.5.0
httpx>=0.27.0
kafka-python-ng>=2.2.2     # Use the -ng fork for better modern compatibility

# ── Config & Validation ────────────────────────────────────────────────────────
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1

# ── Observability ──────────────────────────────────────────────────────────────
prometheus-client>=0.20.0
structlog>=24.1.0

# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
