# ── Install prod deps first ───────────────────────────────────
-r requirements.txt

# ── Testing ───────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
pytest-timeout==2.3.1
anyio[trio]==4.7.0
httpx==0.28.1          # HTTPX AsyncClient for API tests

# ── Type checking ─────────────────────────────────────────────
pyrefly==0.15.0        # Meta's Python type checker
pyright==1.1.390       # alternative / fallback

# ── Stubs for third-party libs ────────────────────────────────
# These install alongside the real packages so Pyrefly can see types
sqlalchemy[mypy]==2.0.36
types-redis==4.6.0.20241004
types-passlib==1.7.7.20240819
types-python-jose==3.3.4.20240106
types-networkx==3.4.2.20241019

# ── Linting ───────────────────────────────────────────────────
ruff==0.8.6
bandit==1.8.0
